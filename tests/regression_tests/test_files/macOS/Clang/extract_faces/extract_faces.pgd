create_box = CreateBoxGeometry {
    width: 10,
    depth: 10,
    height: 5
}

extract_faces = ExtractFaces {}

export_geometry = ExportGeometry {
    path: $< "regression_results/extract_faces/geometry" + count + ".obj"; >$
}

create_box>out -> in<extract_faces>out -> in<export_geometry;

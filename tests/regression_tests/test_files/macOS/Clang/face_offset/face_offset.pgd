create_box = CreateBoxGeometry {
    width: 10,
    depth: 10,
    height: 5
}

face_offset = FaceOffset {
    amount: 1
}

export_geometry = ExportGeometry{
    path: $< "regression_results/face_offset/geometry-" + op.in.offset_tag + "-" + op.count + ".obj"; >$
}

create_box>out -> in<face_offset;
face_offset>inner -> in<export_geometry;
face_offset>outer -> in<export_geometry;
